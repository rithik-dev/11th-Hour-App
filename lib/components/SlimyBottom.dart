import 'package:flutter/material.dart';

class SlimyBottom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("bottom"),
    );
  }
}
