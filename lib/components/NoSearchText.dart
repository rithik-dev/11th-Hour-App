import 'package:flutter/material.dart';

class NoSearchText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("noo search text"),
    );
  }
}
