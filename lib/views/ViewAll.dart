import 'package:flutter/material.dart';

class ViewAll extends StatelessWidget {
  static const id = '/view_all';
  final List courseIds;

  ViewAll({@required this.courseIds});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
